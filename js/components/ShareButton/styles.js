import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  shareIcon: {
    marginTop: 5,
    backgroundColor: 'transparent',
    alignSelf: 'center'
  },
  shareButton: {
    minHeight: 30,
    minWidth: 50,
    marginRight: 5,
  }
});
